{
	"private": true,
	"name": "yay-root",
	"scripts": {
		"dev": "yarn workspace yay run dev",
		"watch": "yarn workspace renderer run watch",
		"build": "yarn create-asars && yarn workspaces foreach -Apti run build:d",
		"package": "yarn create-asars && cross-env ENV=PRODUCTION yarn build:p && cross-env ENV=PRODUCTION yarn workspace yay run electron:dist && yarn postpackage",
		"postpackage": "asar-trim ./build/win-unpacked/resources",
		"build:p": "yarn clean-dist && cross-env ENV=PRODUCTION yarn workspaces foreach -Apti run build:p",
		"clean-dist": "ts-node scripts/clean-dist.ts",
		"create-asars": "ts-node scripts/create-asars.ts",
		"create-installer": "yarn create-online-files-archive && ts-node scripts/create-installer.ts",
		"create-online-files-archive": "ts-node scripts/create-online-files-archive.ts",
		"create-release": "ts-node scripts/create-release.ts"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/depthbomb/yay.git"
	},
	"bugs": {
		"url": "https://github.com/depthbomb/yay/issues"
	},
	"homepage": "https://github.com/depthbomb/yay#readme",
	"workspaces": [
		"packages/app",
		"packages/renderer",
		"packages/shared",
		"packages/nativelib"
	],
	"devDependencies": {
		"@octokit/rest": "^22.0.1",
		"@octokit/types": "^16.0.0",
		"@types/node": "^22.18.13",
		"asar-trim": "^1.2.2",
		"cross-env": "^10.1.0",
		"dotenv": "^17.2.3",
		"electron-builder": "26.0.16",
		"node-gyp": "^11.5.0",
		"ts-morph": "27.0.2",
		"ts-node-maintained": "^10.9.6",
		"typescript": "^5.9.3"
	},
	"packageManager": "yarn@4.10.3"
}
