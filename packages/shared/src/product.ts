import {
	appID,
	clsid,
	author,
	version,
	repoURL,
	dirName,
	nameLong,
	description,
	urlProtocol,
	appUserModelID,
	applicationName,
} from '../../../product.json';

export const product = {
	appID,
	clsid,
	author,
	version,
	repoURL,
	dirName,
	nameLong,
	description,
	urlProtocol,
	appUserModelID,
	applicationName,
} as const;
